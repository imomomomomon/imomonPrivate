package bit.basic.variable;
	/*
	 * Variable Scope(변수 영역)
	 * local -> Stack 지역, 
	 * instance -> Heap 지역, 
	 * static -> Method(static, class) 지역
	 * 
	 * Class(설계도) -> Object(객체) 생성 -> 속성과 기능을 가지고 있음
	*/
public class VariableEx1 {
	
	private static int iStatic = 0;
	
	public static void main(String[] args) {
		int iVariable = 0;
	}
}
