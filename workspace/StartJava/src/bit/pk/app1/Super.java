package bit.pk.app1;
//public(어디서나) >> protected(같은 패키지/상속)
// >> default(같은 패키지) >> private(클래스)
public class Super {
	public int publicData;
	protected int protectedData;
	int defaultData;
	private int privateData;
}
